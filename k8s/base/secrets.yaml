# This is a template file. DO NOT commit actual secrets to git.
# Use kubectl create secret or sealed-secrets in production.
apiVersion: v1
kind: Secret
metadata:
  name: hotel-secrets
  namespace: hotel-management
type: Opaque
stringData:
  database-name: hotel_management
  database-user: postgres
  database-password: CHANGE_ME_IN_PRODUCTION
  jwt-secret: CHANGE_ME_TO_A_LONG_RANDOM_STRING_AT_LEAST_256_BITS
  stripe-api-key: CHANGE_ME_TO_YOUR_STRIPE_KEY
  smtp-password: CHANGE_ME_TO_YOUR_SMTP_PASSWORD
